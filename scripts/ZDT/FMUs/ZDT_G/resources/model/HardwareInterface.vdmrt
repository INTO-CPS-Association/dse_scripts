class HardwareInterface

-- OUTPUT VARIABLES
instance variables

  -- @ interface: type = output, name="f";
  public f : RealPort := new RealPort(0);
  
  -- @ interface: type = output, name="g";
  public g : RealPort := new RealPort(0);

-- PARAMETERS
values
   
  -- @ interface: type = parameter, name="solution_variables";
  public solution_variables : StringPort = new StringPort("[]");
   
end HardwareInterface