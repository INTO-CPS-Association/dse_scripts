class World

operations

-- run a simulation
public run : () ==> ()
run() == 
(
  start(System`g);
  block();
);

-- wait for simulation to finish
block : () ==> ()
block() == skip;

sync per block => false;

values

end World